# Port the relay listens on. Heroku injects $PORT automatically.
PORT=4000

# How long (ms) without a Reachy heartbeat before the robot is shown offline.
REACHY_TIMEOUT_MS=15000

# How many speech turns to keep in memory / send on (re)connect.
HISTORY_LIMIT=50
